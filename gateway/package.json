{
  "name": "fieldwork-gateway",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Zero-dependency Node gateway for Fieldwork and its secret-bearing API proxies.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "NODE_ENV=test node --test"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=20"
  }
}
