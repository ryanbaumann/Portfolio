{
  "name": "trails-ninja",
  "private": true,
  "description": "trails.ninja portfolio container: gateway + demo apps. See docs/ARCHITECTURE.md.",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "build": "node scripts/build-local.mjs",
    "start": "node --env-file=.env gateway/server.js",
    "smoke": "node scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
